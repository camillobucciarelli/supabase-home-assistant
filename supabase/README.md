# Supabase

Run the official Supabase self-hosted Docker stack from Home Assistant.

Open `http://<public_host>:<public_port>` after the add-on starts. By default this is `http://homeassistant.local:8000`.

Read `DOCS.md` before using this in production.
